import React from 'react';
import './spinner.css'

const Spinner = () => {
    return (
        <div>
            <div id="spinner-overlay">
                <div class="spinner"></div>
            </div>
        </div>
    );
};

export default Spinner;